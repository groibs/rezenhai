'use client';
import { useState } from 'react';
import { AppShell, Button, Card, LinkButton, SecondaryButton, SectionTitle, TopBar } from '@/components/ui';
import { demoEvent, templates } from '@/lib/demo-data';
export default function CapaPage() { const [index, setIndex] = useState(0); const [uploaded, setUploaded] = useState(false); const template = templates[index]; return <AppShell><TopBar /><section className="mx-auto grid max-w-7xl gap-8 px-5 py-12 md:grid-cols-[0.9fr_1.1fr] md:px-8"><div><SectionTitle eyebrow="Capa" title="Escolha a cara do convite">Troque a vibe ou simule uma imagem sua, sem backend real.</SectionTitle><div className="mt-8 grid grid-cols-2 gap-3">{templates.map((item, i) => <button key={item.name} onClick={() => setIndex(i)} className={`rounded-2xl border p-5 text-left shadow-card ${index === i ? 'border-brasa-500 bg-brasa-100' : 'border-prata-200 bg-papel'}`}><span className="text-2xl">{item.emoji}</span><p className="mt-3 font-black">{item.name}</p></button>)}</div><div className="mt-6 flex flex-wrap gap-3"><SecondaryButton onClick={() => setIndex((index + 1) % templates.length)}>Trocar vibe</SecondaryButton><Button onClick={() => setUploaded(true)}>Subir imagem</Button><LinkButton href="/eventos/demo/editar">Continuar</LinkButton></div>{uploaded ? <p className="mt-4 rounded-full bg-lima-500 px-4 py-2 text-sm font-bold text-laje-950">Imagem mock adicionada ao convite.</p> : null}</div><Card className="rounded-3xl p-4"><div className={`min-h-[520px] rounded-[30px] p-8 shadow-cover ${template.className}`}><p className="font-mono text-xs font-bold uppercase tracking-[0.08em]">{template.name}</p><h1 className="mt-8 font-display text-6xl leading-none">{demoEvent.title}</h1><p className="mt-5 text-xl font-bold">{demoEvent.date} · {demoEvent.time}</p><p className="mt-2 text-xl font-bold">{demoEvent.local}</p><p className="mt-8 max-w-sm text-lg">{uploaded ? 'Com imagem mock enviada por você.' : demoEvent.description}</p></div></Card></section></AppShell>; }
