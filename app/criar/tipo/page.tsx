'use client';
import { useState } from 'react';
import { AppShell, Card, LinkButton, SectionTitle, TopBar } from '@/components/ui';
import { roleTypes } from '@/lib/demo-data';
export default function TipoPage() { const [selected, setSelected] = useState(roleTypes[0]); return <AppShell><TopBar /><section className="mx-auto max-w-5xl px-5 py-12 md:px-8"><SectionTitle eyebrow="Criar convite" title="Qual é o tipo de rolê?">Escolha uma categoria para começar com uma vibe pronta.</SectionTitle><div className="mt-10 grid gap-4 md:grid-cols-3">{roleTypes.map((type) => <button key={type} onClick={() => setSelected(type)} className={`rounded-2xl border p-6 text-left shadow-card transition ${selected === type ? 'border-brasa-500 bg-brasa-100' : 'border-prata-200 bg-papel hover:border-laje-950'}`}><span className="text-3xl">{type.split(' ')[0]}</span><h2 className="mt-4 text-2xl font-black">{type.replace(/^\S+ /, '')}</h2><p className="mt-2 text-prata-700">Começar com textos, checklist e capa desse clima.</p></button>)}</div><div className="mt-8 flex items-center justify-between rounded-2xl bg-papel p-5 shadow-card"><p className="font-bold">Selecionado: {selected}</p><LinkButton href="/criar/capa">Continuar</LinkButton></div></section></AppShell>; }
