import { AppShell, Card, LinkButton, SecondaryLink, TopBar } from '@/components/ui';
import { checklist, dashboardStats, demoEvent, guests } from '@/lib/demo-data';
export default function DemoDashboardPage() { return <AppShell><TopBar /><section className="mx-auto max-w-7xl px-5 py-10 md:px-8"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="font-mono text-xs font-bold uppercase tracking-[0.08em] text-prata-700">Quem chamou: {demoEvent.organizer}</p><h1 className="mt-2 font-display text-6xl leading-none">{demoEvent.title}</h1><p className="mt-3 text-prata-700">{demoEvent.date} · {demoEvent.time} · {demoEvent.local}</p></div><div className="flex flex-wrap gap-3"><SecondaryLink href="/e/churras-dos-amigos">Ver página pública</SecondaryLink><SecondaryLink href="/eventos/demo/editar">Editar convite</SecondaryLink><LinkButton href="/dashboard/demo/fotos">Fotos</LinkButton></div></div><div className="mt-8 grid gap-3 md:grid-cols-7">{dashboardStats.map(([label, value]) => <div key={label} className={`rounded-2xl p-5 shadow-card ${label === 'Confirmados' ? 'bg-lima-500' : 'bg-papel'}`}><p className="font-mono text-[11px] font-bold uppercase tracking-[0.08em] text-prata-700">{label}</p><p className="mt-2 font-mono text-4xl font-black tabular-nums">{value}</p></div>)}</div><div className="mt-6 grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card><h2 className="text-2xl font-black">Lista de convidados</h2><div className="mt-4 divide-y divide-prata-200">{guests.map((guest) => <div key={guest.name} className="grid gap-2 py-4 md:grid-cols-4 md:items-center"><p className="font-extrabold">{guest.name}</p><p className="text-sm font-bold text-prata-700">{guest.status}</p><span className={`w-fit rounded-full px-3 py-1 text-xs font-extrabold ${guest.pix === 'Pix marcado' ? 'bg-lima-500 text-laje-950' : 'bg-brasa-100 text-laje-950'}`}>{guest.pix}</span><p className="text-sm text-prata-700">{guest.item || 'Sem item'}</p></div>)}</div></Card><div className="space-y-6"><Card className="bg-brasa-100"><h2 className="text-2xl font-black">Faltam resolver</h2><ul className="mt-4 list-disc space-y-2 pl-5 font-bold text-laje-700"><li>Confirmar Pix pendente do João e da Rafa.</li><li>Definir quem leva descartáveis.</li><li>Convidar pendentes do grupo.</li></ul></Card><Card><h2 className="text-2xl font-black">Checklist</h2><div className="mt-4 space-y-2">{checklist.map((item, index) => <div key={item} className="flex items-center justify-between rounded-2xl bg-creme-25 p-3 font-bold"><span>{item}</span><span className="text-sm text-prata-700">{index < 4 ? 'Resolvido' : 'Livre'}</span></div>)}</div></Card><Card><h2 className="text-2xl font-black">Ações rápidas</h2><div className="mt-4 flex flex-wrap gap-3"><SecondaryLink href="/eventos/demo/organizar">Adicionar ao convite</SecondaryLink><SecondaryLink href="/eventos/demo/preview">Copiar link</SecondaryLink></div></Card></div></div></section></AppShell>; }
