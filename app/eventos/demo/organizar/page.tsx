'use client';
import { useState } from 'react';
import { AppShell, Card, LinkButton, SecondaryLink, TopBar } from '@/components/ui';
const cards = ['Pix', 'Quem leva o quê', 'Perguntas', 'Acompanhantes', 'Aprovação', 'Fotos'];
export default function OrganizarPage() { const [enabled, setEnabled] = useState<Record<string, boolean>>({ Pix: true, 'Quem leva o quê': true, Fotos: true }); return <AppShell><TopBar /><section className="mx-auto max-w-7xl px-5 py-10 md:px-8"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="font-mono text-xs font-bold uppercase tracking-[0.08em] text-prata-700">Organiza</p><h1 className="mt-2 font-display text-6xl leading-none">Adicionar ao convite</h1><p className="mt-4 text-prata-700">Ligue ou desligue recursos mockados para ver a prévia.</p></div><SecondaryLink href="/eventos/demo/preview">Prévia</SecondaryLink></div><div className="mt-8 grid gap-4 md:grid-cols-3">{cards.map((card) => <Card key={card}><div className="flex items-start justify-between gap-4"><div><h2 className="text-2xl font-black">{card}</h2><p className="mt-2 text-prata-700">Funciona com estado local por enquanto.</p></div><button onClick={() => setEnabled((old) => ({ ...old, [card]: !old[card] }))} className={`rounded-full px-4 py-2 text-sm font-black ${enabled[card] ? 'bg-lima-500 text-laje-950' : 'bg-prata-200 text-laje-700'}`}>{enabled[card] ? 'Ativo' : 'Inativo'}</button></div>{card === 'Pix' ? <p className="mt-4 text-sm font-bold text-prata-700">O Pix vai direto para o organizador do evento. O Rezenhaí só ajuda a organizar.</p> : null}</Card>)}</div><LinkButton href="/eventos/demo/preview" className="mt-8">Ver prévia</LinkButton></section></AppShell>; }
