'use client';
import { useState } from 'react';
import { AppShell, Button, Card, LinkButton, SecondaryButton, SecondaryLink, Toast } from '@/components/ui';
import { checklist, demoEvent } from '@/lib/demo-data';
const statuses = ['Eu colo', 'Talvez', 'Não colo'];
export default function PublicInvitePage() { const [status, setStatus] = useState(''); const [step, setStep] = useState(0); const [toast, setToast] = useState(''); const start = (value: string) => { setStatus(value); setStep(1); }; const copyPix = () => { navigator.clipboard?.writeText(demoEvent.pixKey); setToast('Pix copiado.'); setTimeout(() => setToast(''), 1600); }; return <AppShell tone="bg-laje-950 text-creme-50"><section className="mx-auto grid min-h-screen max-w-7xl gap-8 px-5 py-8 md:grid-cols-[1fr_0.85fr] md:px-8"><div className="flex flex-col justify-center"><SecondaryLink href="/" className="mb-8 w-fit border-creme-50/20 bg-creme-50/10 text-creme-50">Rezenhaí</SecondaryLink><p className="font-mono text-xs font-bold uppercase tracking-[0.08em] text-lima-500">Quem chamou: {demoEvent.organizer}</p><h1 className="mt-4 font-display text-7xl leading-none md:text-8xl">{demoEvent.title}</h1><p className="mt-6 text-xl text-creme-50/80">{demoEvent.date} · {demoEvent.time} · {demoEvent.local}</p><p className="mt-4 max-w-xl text-lg text-creme-50/70">{demoEvent.description}</p><SecondaryLink href="/e/churras-dos-amigos/fotos" className="mt-8 w-fit border-creme-50/20 bg-creme-50/10 text-creme-50">Ver fotos</SecondaryLink></div><Card className="my-auto bg-papel text-laje-950"><h2 className="text-4xl font-black">Você vai colar?</h2><p className="mt-3 text-prata-700">Responde rapidinho. Depois a gente pergunta nome, Pix e se você leva algo.</p><div className="mt-6 grid gap-3">{statuses.map((item) => <Button key={item} onClick={() => start(item)} className={item === 'Eu colo' ? 'bg-lima-500 text-laje-950 hover:bg-lima-300' : item === 'Talvez' ? 'bg-prata-300 text-laje-950 hover:bg-prata-200' : 'bg-laje-700 hover:bg-laje-600'}>{item}</Button>)}</div><div className="mt-8 rounded-2xl bg-creme-25 p-4"><p className="font-bold">Contribuição: {demoEvent.contribution}</p><p className="mt-2 text-sm text-prata-700">O Pix vai direto para o organizador do evento. O Rezenhaí só ajuda a organizar.</p></div></Card></section>{step > 0 ? <div className="fixed inset-0 z-40 flex items-end bg-laje-950/60 p-3 md:items-center md:justify-center"><div className="max-h-[92vh] w-full max-w-xl overflow-auto rounded-3xl bg-papel p-6 text-laje-950 shadow-pop"><p className="font-mono text-xs font-bold uppercase tracking-[0.08em] text-prata-700">{status}</p>{step === 1 ? <div><h2 className="mt-2 text-3xl font-black">Como colocamos seu nome na lista?</h2><input className="mt-5 w-full rounded-2xl border-prata-200 bg-creme-25 p-4 font-bold" placeholder="Seu nome" autoFocus /><Button onClick={() => setStep(2)} className="mt-4 w-full">Continuar</Button></div> : null}{step === 2 ? <div><h2 className="text-3xl font-black">Esse rolê tem contribuição de {demoEvent.contribution}.</h2><p className="mt-4 text-prata-700">Chave Pix: <strong>{demoEvent.pixKey}</strong></p><p className="mt-4 rounded-2xl bg-creme-25 p-4 text-sm font-bold text-prata-700">O Pix vai direto para o organizador do evento. O Rezenhaí só ajuda a organizar.</p><div className="mt-5 grid gap-3"><SecondaryButton onClick={copyPix}>Copiar Pix</SecondaryButton><Button onClick={() => setStep(3)}>Já fiz o Pix</Button><SecondaryButton onClick={() => setStep(3)}>Pagar depois</SecondaryButton></div></div> : null}{step === 3 ? <div><h2 className="text-3xl font-black">Quer levar algo?</h2><div className="mt-5 grid gap-2">{checklist.map((item) => <button key={item} className="rounded-2xl bg-creme-25 p-3 text-left font-bold hover:bg-lima-100">{item}</button>)}</div><Button onClick={() => setStep(4)} className="mt-4 w-full">Continuar</Button></div> : null}{step === 4 ? <div><h2 className="text-3xl font-black">Fechou. Você está na lista.</h2><p className="mt-4 text-prata-700">Sua confirmação de presença ficou salva neste navegador em modo demo.</p><div className="mt-6 grid gap-3"><SecondaryButton onClick={() => setStep(0)}>Voltar ao convite</SecondaryButton><LinkButton href="/criar/tipo">Criar meu convite</LinkButton><SecondaryLink href="/e/churras-dos-amigos/fotos">Ver fotos</SecondaryLink></div></div> : null}</div></div> : null}<Toast message={toast} /></AppShell>; }
