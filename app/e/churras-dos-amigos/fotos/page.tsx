'use client';
import { useState } from 'react';
import { AppShell, Button, Card, SecondaryButton, SecondaryLink } from '@/components/ui';
import { mockPhotos } from '@/lib/demo-data';
export default function PublicPhotosPage() { const [show, setShow] = useState(true); const [sent, setSent] = useState(false); const photos = sent || show ? mockPhotos : []; return <AppShell><section className="mx-auto max-w-7xl px-5 py-8 md:px-8"><SecondaryLink href="/e/churras-dos-amigos">Voltar ao convite</SecondaryLink><div className="mt-10 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="font-mono text-xs font-bold uppercase tracking-[0.08em] text-prata-700">Galeria</p><h1 className="mt-2 font-display text-6xl leading-none">Fotos da resenha</h1></div><div className="flex flex-wrap gap-3"><Button onClick={() => setSent(true)}>Enviar foto</Button><SecondaryButton onClick={() => setSent(true)}>Tirar foto</SecondaryButton><SecondaryButton onClick={() => setSent(true)}>Escolher da galeria</SecondaryButton><SecondaryButton onClick={() => setShow(!show)}>{photos.length ? 'Ver estado vazio' : 'Ver fotos mockadas'}</SecondaryButton></div></div>{photos.length ? <div className="mt-8 grid gap-4 md:grid-cols-4">{photos.map((photo, index) => <Card key={photo} className="aspect-square bg-holo"><p className="font-mono text-sm font-black">0{index + 1}</p><h2 className="mt-20 text-2xl font-black">{photo}</h2></Card>)}</div> : <Card className="mt-8"><h2 className="text-2xl font-black">Ainda não tem foto por aqui.</h2><p className="mt-3 text-prata-700">Quando a galera enviar, elas aparecem neste espaço.</p></Card>}</section></AppShell>; }
